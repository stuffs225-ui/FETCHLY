# توثيق المشروع الكامل — منصة التوريد العالمي

> ملف مرجعي شامل لمراجعة كل تفاصيل المشروع: الفكرة، التصميم، الألوان، الصفحات، لوحة التحكم، نموذج البيانات، والقيود الحالية. الهدف منه تسهيل المراجعة وتحديد ما يحتاج تحسين قبل الإطلاق الفعلي.

---

## 1. فكرة المشروع (Business Model)

منصة توريد عالمي (Global Sourcing) لمنشأة سعودية:

- العميل (شركة أو فرد) يحتاج منتجًا يصعب توفره محليًا.
- يرسل طلبه عبر الموقع **بدون تسجيل أو إنشاء حساب** — يكفي صورة، رابط، رقم قطعة (Part Number)، أو وصف مبسّط.
- الفريق الداخلي يراجع الطلب، يبحث عن المصدر المناسب عالميًا (أمريكا، بريطانيا، أوروبا، آسيا... أو أي مكان)، ويجهز عرض سعر.
- عرض السعر يصل للعميل عبر البريد الإلكتروني، وأي متابعة تجارية تكمل مباشرة عبر البريد.

**ما هو ليس كذلك عمدًا:** ليس متجرًا إلكترونيًا، لا يوجد كتالوج منتجات عام، لا يوجد حساب عميل أو تسجيل دخول للعملاء، لا سلة شراء، لا دفع إلكتروني عبر الموقع. القسم الوحيد المحمي بكلمة مرور هو لوحة تحكم الإدارة الداخلية.

**الفلسفة التصميمية:** "أرسل لنا ما لديك، ونحن نكمل الباقي." — الموقع نفسه هو تجربة الثقة (لا يوجد كتالوج يثبت الجدية، فالتصميم والوضوح والاحترافية هي ما تبني الثقة).

---

## 2. اللغة والاتجاه

- **العربية هي اللغة الافتراضية والرئيسية** — التصميم RTL من الأساس (ليس ترجمة لموقع إنجليزي).
- **الإنجليزية نسخة كاملة بديلة** عبر مبدّل اللغة في الهيدر (العربية | EN).
- التبديل يغيّر فعليًا اتجاه الصفحة (`dir="rtl"` / `dir="ltr"`)، الخط، ومحاذاة كل عنصر عبر خصائص CSS المنطقية (`ps-`, `pe-`, `text-start`, إلخ) وليس فقط ترجمة النصوص.
- لوحة التحكم (Admin) **عربية فقط RTL** — لا يوجد بها مبدّل لغة (قرار مقصود، لأنها للفريق الداخلي فقط).

---

## 3. المكدس التقني (Tech Stack)

| الطبقة | التقنية |
|---|---|
| الواجهة | React 19 + TypeScript + Vite |
| التنسيق | Tailwind CSS v4 |
| التوجيه | React Router (BrowserRouter) |
| التعدد اللغوي | نظام مخصص (Context + قواميس ar/en) — بدون مكتبة خارجية |
| الرسم ثلاثي الأبعاد | three.js + @react-three/fiber + @react-three/drei (Lazy-loaded) |
| توليد PDF | jsPDF + html2canvas (تصوير DOM فعليًا لضمان شكل عربي RTL صحيح) |
| الأيقونات | lucide-react |
| الخطوط | Google Fonts: Cairo / Tajawal (عربي)، Inter (إنجليزي)، JetBrains Mono (أرقام) |

---

## 4. نظام التصميم (Design System)

### 4.1 الألوان

الخلفية داكنة (Dark Mode فقط للموقع العام والإدارة)، مع لمسة ذهبية فاخرة بدل الأزرق التقني المعتاد.

| الاسم | القيمة | الاستخدام |
|---|---|---|
| `ink` (الخلفية الأساسية) | `#0b0e14` | خلفية الصفحة |
| `surface` | `#12161f` | خلفية أقسام بديلة (شرائط، فواصل) |
| `card` | `#171b26` | خلفية البطاقات |
| `card-light` | `#1c2230` | تدرّج فاتح للبطاقات |
| `border` | `#242a3a` | حدود أساسية |
| `border-light` | `#2f3648` | حدود العناصر التفاعلية (حقول، أزرار) |
| **`gold`** (اللون الأساسي/البراند) | `#c9a227` | الأزرار الأساسية، الروابط المهمة، الأيقونات المميزة |
| `gold-light` | `#e0bd4f` | حالة hover على الذهبي |
| `navy` | `#1b2a4a` | تدرّجات، خلفيات ثانوية (شعار، أزرار داكنة) |
| `navy-light` | `#2b3f68` | تمييز حالة "تم الإرسال" في الشارات |
| `emerald` | `#1e9e6b` | حالات النجاح/الموافقة/التحقق |
| `danger` | `#e5484d` | أخطاء، رفض |
| `text` | `#f5f3ee` | النص الأساسي (أبيض دافئ، ليس أبيض نقي) |
| `text-muted` | `#9ca3b5` | النص الثانوي |

**ألوان خاصة بقالب PDF فقط** (خلفية بيضاء احترافية، مختلفة عن الوضع الداكن للموقع):
`paper #faf8f3`, `paper-card #ffffff`, `paper-border #e7e2d6`, نص PDF أساسي `#1a1d24`، نص ثانوي `#5b6270`.

> ملاحظة تصميم: تم تفادي اللونين الأخضر/الأبيض التقليديين لعلم السعودية كهوية أساسية (تفاديًا لشكل "حكومي رسمي")، واستُخدم الذهبي كبديل فاخر مع لمسة أخضر زمردي محدودة لحالات التحقق فقط.

### 4.2 الخطوط

- **العربية:** Cairo (أساسي) → Tajawal (احتياطي) — أوزان 400/500/600/700/800.
- **الإنجليزية:** Inter — أوزان 400/500/600/700/800.
- **الأرقام/المعرفات:** JetBrains Mono (أرقام الطلبات، أرقام عروض الأسعار، الأسعار).
- يتغيّر الخط تلقائيًا حسب `<html lang="ar|en">`.

### 4.3 الحركة والانتقالات (Motion)

- `fade-up` (0.8s): ظهور تدريجي للعناصر مع حركة صعود بسيطة — مستخدم في الهيرو.
- `fade-in` (0.6s): ظهور بسيط.
- `drift` (24-34s): حركة عائمة بطيئة جدًا للتوهجات الخلفية (Glow orbs) — أداء خفيف.
- `marquee-rtl` (40s): شريط متحرك (لو استُخدم لاحقًا لعرض الأسواق).
- **يُحترم `prefers-reduced-motion`**: كل الحركات تُعطَّل تلقائيًا لمن يفعّل "تقليل الحركة" في نظامه.

### 4.4 القطعة البصرية المميزة: الكرة الأرضية ثلاثية الأبعاد

- في الهيرو (الصفحة الرئيسية): كرة أرضية wireframe بتقنية three.js، السعودية نقطة ذهبية مضيئة في المنتصف، وخطوط مقوّسة (Arcs) متحركة تصل لأمريكا/بريطانيا/ألمانيا/إيطاليا/اليابان/كوريا الجنوبية/الصين، مع نقطة نبض (pulse) تتحرك على كل مسار بشكل مستمر لإيحاء "شحنة قيد النقل".
- **قابلة للتدوير يدويًا** (OrbitControls بدون تكبير/تحريك، فقط تدوير خفيف).
- **أداء وحماية:** يُحمَّل هذا المكوّن (three.js وزنه كبير) بشكل Lazy فقط، ولا يظهر إطلاقًا على الشاشات الصغيرة (أقل من 820px) أو الأجهزة بدون WebGL — بديل يظهر عندها: رسم SVG مبسّط بنفس الفكرة (نقطة مركزية + مسارات متحركة بـ CSS) خفيف جدًا على الأداء.
- fallback إضافي عبر Error Boundary: لو فشل تحميل المكوّن ثلاثي الأبعاد لأي سبب، يظهر نفس البديل الخفيف بدل شاشة فارغة أو كسر الصفحة.

### 4.5 مكوّنات الواجهة المشتركة (UI Kit)

`Button` (أساسي ذهبي / ثانوي محدد / داكن / شبح / خطر) · `Card` / `CardHover` · `Input` / `Textarea` / `Select` (مع تسمية موحدة uppercase صغيرة) · `Accordion` (للأسئلة الشائعة) · `DataTable` (بحث/ترتيب/صفحات — تستخدمها كل جداول الإدارة) · `Drawer` (لوحة جانبية تنزلق من الاتجاه الصحيح حسب RTL/LTR) · `StatusBadge` (شارات ملوّنة لكل حالة) · `FileDropzone` (سحب وإفلات الملفات) · `DonutChart` (رسم دائري بسيط SVG بدون مكتبة).

---

## 5. خريطة الموقع العام (Public Site)

| الصفحة | المسار | المحتوى الأساسي |
|---|---|---|
| الرئيسية | `/` | 11 قسمًا (تفصيل أدناه) |
| كيف نعمل | `/how-it-works` | رحلة توريد من 8 خطوات + أسئلة شائعة |
| ماذا نوفر | `/what-we-source` | 12 تصنيف توضيحي + "منتج آخر؟" |
| من نحن | `/about` | لماذا وجدنا / ماذا نفعل / رؤيتنا / رسالتنا / كيف نعمل |
| الموثوقية والامتثال | `/trust` | بطاقات الاعتمادات الرسمية + فقرة الامتثال (SABER/SFDA) |
| الأسئلة الشائعة | `/faq` | 8 أسئلة (قابلة للاستبدال من لوحة التحكم) |
| تواصل معنا | `/contact` | بيانات التواصل + نموذج رسالة ثانوي |
| اطلب عرض سعر | `/request` | نموذج الطلب الكامل (القسم الأهم في الموقع) |
| صفحات قانونية | `/legal/privacy`, `/legal/terms`, `/legal/cookies`, `/legal/complaints`, `/legal/disclaimer` | محتوى قانوني حقيقي (وليس Lorem ipsum) |

### 5.1 تفصيل أقسام الصفحة الرئيسية (بالترتيب)

1. **Hero** — عنوان رئيسي + الكرة ثلاثية الأبعاد + CTA أساسي "اطلب عرض سعر" وثانوي "كيف تعمل الخدمة؟"
2. **شريط الثقة المبكر** — "منشأة سعودية مسجلة وموثقة" + أسماء الاعتمادات (بدون أرقام مزيّفة).
3. **"ما تحتاج تعرف كل تفاصيل المنتج"** — 4 بطاقات (صورة / رابط / Part Number / وصف) + خاتمة "ونحن نكمل الباقي."
4. **كيف تعمل الخدمة** — 3 خطوات فقط (مبسّطة عمدًا عن صفحة "كيف نعمل" المفصّلة).
5. **العالم سوقك** — قائمة الأسواق (أمريكا، بريطانيا، ألمانيا، إيطاليا، أوروبا، اليابان، كوريا الجنوبية، الصين، وأي مكان آخر).
6. **ماذا يمكن أن نوفر لك** — 12 تصنيف + بطاقة "ما لقيت تصنيف منتجك؟".
7. **نبحث عن الأشياء التي يصعب إيجادها** — 9 أمثلة توضيحية.
8. **لماذا نحن** — 7 مزايا حقيقية (مصادر عالمية، بدون تسجيل، طلب بسيط، عرض سعر واضح، مصادر موثوقة، متطلبات الاستيراد، تواصل مباشر) — **بدون أي أرقام أو ادعاءات مبالغ فيها**.
9. **نماذج من طلبات قمنا بتوريدها** — 3 حالات توضيحية (زيت متخصص من أمريكا، قطعة صناعية من بريطانيا، جهاز متخصص من أوروبا) — قابلة للإضافة/التعديل من لوحة التحكم، **بدون أسماء عملاء وهمية**.
10. **تعامل واضح. بيانات يمكن التحقق منها** — دعوة لصفحة الموثوقية.
11. **نموذج طلب عرض السعر الكامل** — مضمّن مباشرة في نهاية الرئيسية (بالإضافة لصفحته المستقلة `/request`).

### 5.2 نموذج طلب عرض السعر — التفاصيل

**بدون تسجيل دخول أو حساب إطلاقًا.**

- **بيانات التواصل:** الاسم، اسم الشركة (اختياري)، البريد الإلكتروني، الجوال/واتساب، المدينة.
- **بيانات المنتج:** اسم المنتج (إجباري)، الكمية، وصف/ملاحظات — وحقول إضافية *قابلة للطي* (Progressive Disclosure): العلامة التجارية، الموديل، Part Number، رابط المنتج.
- **تفضيل المصدر:** أزرار اختيار (أفضل مصدر متاح / أمريكا / بريطانيا / أوروبا / آسيا / دولة أخرى) — الافتراضي "أفضل مصدر متاح".
- **المرفقات:** سحب وإفلات صور/لقطات شاشة/PDF (حتى 5 ملفات، 10MB لكل ملف).
- **قسم التسليم** (قابل للطي، اختياري بالكامل): مدينة التسليم، تاريخ مطلوب، مدى الأولوية.
- **موافقة صريحة على سياسة الخصوصية** (checkbox غير مُفعّل مسبقًا) مع رابط مباشر للسياسة — **إجباري** للإرسال.
- عند الإرسال: يتولّد رقم طلب فريد بصيغة `REQ-2026-00001`، يظهر للعميل مباشرة، وتُرسل رسالة تأكيد (راجع القسم 8 لفهم آلية البريد الفعلية).

---

## 6. لوحة التحكم (Admin — `/admin`)

محمية بكلمة مرور واحدة (متغيّر بيئة `VITE_ADMIN_PASSWORD`، الافتراضي `admin@2026` — **يجب تغييره قبل أي نشر فعلي**، راجع القسم 9).

### 6.1 عناصر القائمة الجانبية

الرئيسية (Dashboard) · الطلبات · عروض الأسعار · المنتجات المحفوظة · حالات التوريد · الأسئلة الشائعة · الشهادات والتوثيق · محتوى الموقع · إعدادات الشركة · إعدادات البريد · المستخدمون.

### 6.2 لوحة المعلومات (Dashboard)

مؤشرات: طلبات جديدة · قيد المراجعة · قيد التسعير · تم إرسال العرض · تمت الموافقة · مرفوضة · مغلقة — بالإضافة لـ: طلبات/عروض هذا الشهر، إجمالي قيمة العروض، قيمة العروض المقبولة، معدل الفوز (Win Rate)، متوسط قيمة العرض. رسم دائري لتوزيع الطلبات حسب الحالة، وجدول لأحدث الطلبات.

### 6.3 الطلبات

جدول قابل للبحث والفلترة (حسب الحالة، نطاق تاريخ). فتح أي طلب يعرض: بيانات العميل والمنتج كاملة، معاينة/تحميل المرفقات، حالة الطلب (8 حالات، راجع القسم 7)، الوكيل المسؤول، ملاحظات داخلية (غير مرئية للعميل)، سجل نشاط زمني (Audit Trail)، وزر "إنشاء عرض سعر" مباشر.

### 6.4 عروض الأسعار (القلب التشغيلي للنظام)

- **ترقيم تلقائي:** `QT-2026-0001` (رقم مستقل عن رقم الطلب لكن مرتبط به).
- **حقول العرض:** لغة العرض (عربي/إنجليزي — **مستقلة تمامًا عن لغة الموقع**)، العملة (SAR افتراضيًا، USD/GBP/EUR)، صلاحية العرض (7/15/30 يوم أو تاريخ مخصص).
- **بيانات العميل:** تُملأ تلقائيًا من الطلب، قابلة للتعديل لهذا العرض تحديدًا فقط.
- **بنود العرض:** جدول بنود لا محدود (اسم، وصف، كمية، سعر الوحدة) مع **حساب تلقائي فوري**: الإجمالي = الكمية × سعر الوحدة، ويقترح أسماء بنود سبق تسعيرها (Autocomplete من "المنتجات المحفوظة").
- **الضريبة:** تبديل نعم/لا لتطبيق ضريبة القيمة المضافة، بنسبة **قابلة للتعديل (افتراضي 15%)** من إعدادات الشركة أو لكل عرض على حدة.
  - `الإجمالي الفرعي = مجموع (الكمية × السعر) لكل البنود`
  - `الضريبة = الإجمالي الفرعي × النسبة` (إن كانت مفعّلة، وإلا صفر)
  - `الإجمالي الكلي = الإجمالي الفرعي + الضريبة`
- **الشروط التجارية:** مدة التوريد، شروط الدفع (مع اقتراحات محفوظة قابلة للإدراج بضغطة)، موقع التسليم، الضمان، ملاحظات، الشروط والأحكام (نفس فكرة الاقتراحات المحفوظة).
- **توليد PDF:** زر "معاينة PDF" (يفتح في تبويب جديد) و"تحميل PDF" — ملف A4 احترافي مطابق للهوية، بعنوان الشركة والشعار، جدول البنود، الإجماليات، الشروط، وتذييل ببيانات الشركة القانونية. **يدعم عربي RTL وإنجليزي بشكل صحيح 100%** لأن التوليد يعتمد على تصوير DOM فعلي (html2canvas) بدل رسم نص مباشر (يتجنب مشاكل تشكيل الحروف العربية التي تحصل مع مكتبات PDF التقليدية).
- **الإرسال للعميل:** معاينة نص الرسالة (قابل للتعديل)، إرسال (محاكاة — راجع القسم 8)، تحديث تلقائي لحالة الطلب المرتبط إلى "تم إرسال عرض السعر"، وتسجيل الحدث في سجل النشاط.
- **النسخ المعدّلة (Revisions):** لا يُحذف أي تعديل — بدلاً منه يُنشأ عرض جديد بنفس الرقم الأساسي ولاحقة `-R1`, `-R2`... مع الاحتفاظ بكل نسخة سابقة وحالتها وتاريخها كاملة.

### 6.5 الإدارة الأخرى

- **المنتجات المحفوظة:** تُحفظ تلقائيًا من كل عرض سعر (اسم، وصف، آخر سعر، تاريخ آخر تسعير) لتسريع تكرار التسعير.
- **حالات التوريد / الأسئلة الشائعة:** إدارة كاملة (إضافة/تعديل/حذف/نشر أو إخفاء) — تنعكس فورًا على الموقع العام.
- **الشهادات والتوثيق:** لكل اعتماد (سجل تجاري، منصة الأعمال، ضريبة القيمة المضافة، الزكاة، العنوان الوطني، بلدي، أيزو، أخرى): الجهة المصدرة، الرقم، تاريخ الإصدار/الانتهاء، رابط تحقق، رفع مستند، وتبديل الظهور في الموقع العام.
- **محتوى الموقع:** التحكم بنص العنوان الرئيسي (Hero) والوصف الفرعي بشكل مستقل بالعربي والإنجليزي (يترك فارغًا = يستخدم النص الافتراضي).
- **إعدادات الشركة:** الاسم (عربي/إنجليزي)، رفع الشعار (عام + نسخة عربية بديلة اختيارية)، السجل التجاري، الرقم الضريبي، شهادة الزكاة، رقم منصة الأعمال، العنوان الوطني، رخصة بلدي، الهاتف/واتساب/البريد، نسبة الضريبة الافتراضية، العملة الافتراضية.
- **إعدادات البريد:** اسم/بريد المرسل، بريد الرد، بريد الإشعارات الداخلية، قوالب البريد (تأكيد استلام الطلب + إرسال عرض السعر) بالعربي والإنجليزي كل على حدة، مع متغيرات ديناميكية (`{{name}}`, `{{requestNumber}}`, `{{quotationNumber}}`, `{{validUntil}}`) — وفي أسفل الصفحة **سجل البريد الإلكتروني الكامل** (كل رسالة أُرسلت أو حوكيت، تاريخها، حالتها).
- **المستخدمون:** حاليًا مستخدم إداري واحد بصلاحية كاملة، مع بنية بيانات جاهزة لإضافة دور "مبيعات/تسعير" لاحقًا دون الحاجة لإعادة بناء.

---

## 7. حالات الطلب (Status Pipeline)

طلب جديد → قيد المراجعة → قيد التسعير → تم إرسال عرض السعر → العميل مهتم → تمت الموافقة / مرفوض → مغلق.

كل تغيير حالة يُسجَّل في سجل التدقيق (Audit Log) مع الوقت والمُنفِّذ.

---

## 8. آلية البريد الإلكتروني — **مهم جدًا للمراجعة**

⚠️ **لا يوجد مزود بريد فعلي مربوط حاليًا** (لا SMTP ولا Resend ولا أي خدمة إرسال حقيقية) لأنه لا توجد بيئة خلفية (Backend) في هذه المرحلة.

بدلاً من ذلك:

- عند إرسال أي بريد (تأكيد استلام طلب، أو إرسال عرض سعر، أو رسالة تواصل)، يتم **تسجيله فعليًا** بكامل تفاصيله (المُرسَل إليه، الموضوع، النص، اسم المرفق) في **"سجل البريد الإلكتروني"** الظاهر داخل لوحة التحكم (إعدادات البريد ← أسفل الصفحة).
- هذا يُحاكي التجربة الكاملة (العميل يرى شاشة "تم الإرسال"، الإدارة ترى السجل) **لكن لا يصل بريد فعلي لصندوق وارد أي أحد.**
- الكود مبني بحيث تبديل هذا لاحقًا بمزود حقيقي هو **تعديل ملف واحد فقط**: `src/lib/emailService.ts`.

**هذا أهم نقطة يجب تأكيدها قبل أي إطلاق فعلي للموقع** — بدونها لن يصل أي بريد حقيقي للعملاء.

---

## 9. نقاط أخرى تحتاج **تدخل فعلي منك قبل الإطلاق**

| البند | الوضع الحالي | المطلوب |
|---|---|---|
| اسم الشركة | `[اسم الشركة]` / `[COMPANY_NAME_EN]` (نص Placeholder حرفي بين أقواس) | إدخال الاسم الفعلي من إعدادات الشركة |
| الشعار | لا يوجد (دائرة بحرف أول من الاسم) | رفع الشعار الفعلي |
| السجل التجاري / الرقم الضريبي / الزكاة / منصة الأعمال / العنوان الوطني / رخصة بلدي | كل الحقول تعرض `[CR_NUMBER]` إلخ، أو "قيد الإعداد" في الموقع العام | إدخال الأرقام الحقيقية — **لم يتم اختلاق أي رقم** |
| كلمة مرور لوحة التحكم | قيمة افتراضية تجريبية `admin@2026` (متغيّر بيئة) | **يجب تغييرها إلزاميًا قبل النشر** |
| قاعدة البيانات | لا توجد قاعدة بيانات حقيقية — التخزين في المتصفح (localStorage + IndexedDB) | ربط قاعدة بيانات وخادم فعلي (Node/Postgres أو ما شابه) قبل الإطلاق التجاري — البنية الحالية للـ Repositories مصمَّمة لتسهيل هذا التبديل |
| المصادقة الإدارية | تحقق بسيط من كلمة المرور في المتصفح فقط | استبدالها بمصادقة خادم حقيقية (Session/JWT) في مرحلة الإنتاج |
| البريد الإلكتروني | محاكاة/سجل فقط (راجع القسم 8) | ربط مزود بريد حقيقي |
| الأسواق العالمية / التصنيفات | القوائم المذكورة (أمريكا، بريطانيا، ألمانيا...) هي **أمثلة توضيحية غير حصرية** كما نص عليها المتطلب الأصلي | يمكن تعديلها بحرية، الصياغة الحالية توضح صراحة أنها غير حصرية |
| الأرقام والإحصاءات | **لا توجد أي أرقام أو ادعاءات مبالغ فيها** (لا "+1000 عميل"، لا "%99 رضا") بحسب طلبك الصريح | إن رغبت بإضافة أرقام حقيقية لاحقًا فالمكان جاهز فقط عند توفر بيانات فعلية |

---

## 10. هيكل الملفات (للمطورين)

```
src/
├── i18n/                  # القواميس (ar.ts / en.ts) + Context اللغة
├── lib/
│   ├── types.ts           # كل أنواع البيانات (Request, Quotation, Credential...)
│   ├── storage.ts         # طبقة تخزين عامة (localStorage) + إشعارات التغيير
│   ├── repo.ts             # المستودعات (Repositories) لكل كيان + بيانات أولية
│   ├── attachments.ts      # تخزين الملفات في IndexedDB
│   ├── emailService.ts     # "إرسال" البريد (محاكاة، راجع القسم 8)
│   ├── pdf.ts              # تحويل DOM إلى PDF (html2canvas + jsPDF)
│   └── adminAuth.ts        # حارس كلمة مرور الإدارة (مؤقت، راجع القسم 9)
├── components/
│   ├── ui/                # مكوّنات عامة (زر، بطاقة، جدول، حقول...)
│   ├── layout/             # الهيدر، الفوتر، الشريط الجانبي للإدارة، واتساب...
│   ├── three/              # الكرة الأرضية + البديل الخفيف
│   └── admin/              # مكوّنات خاصة بالإدارة (معاينة مرفق، قالب PDF...)
├── pages/
│   ├── public/             # كل الصفحات العامة + أقسام الرئيسية في public/home/
│   ├── legal/              # الصفحات القانونية + محتواها
│   └── admin/              # كل صفحات لوحة التحكم
└── App.tsx                 # التوجيه الكامل (Public routes + Admin lazy-loaded)
```

---

## 11. كيفية التشغيل محليًا

```bash
npm install
npm run dev        # تشغيل للتطوير
npm run build      # بناء نسخة الإنتاج
npm run preview    # معاينة نسخة الإنتاج
```

انسخ `.env.example` إلى `.env.local` وغيّر `VITE_ADMIN_PASSWORD` قبل أي استخدام جدي.

---

## 12. أفكار محتملة للتحسين (اقتراحات، وليست تنفيذًا)

هذه نقاط لم تُنفَّذ أو نُفِّذت بأبسط شكل ممكن ضمن حدود الوقت، ومفتوحة لتقييمك:

1. **أرقام ومقاييس اجتماعية حقيقية** — بعد جمع بيانات فعلية (عدد طلبات، متوسط زمن الرد...)، يمكن إضافتها في الصفحة الرئيسية لزيادة الثقة.
2. **تحسين محركات البحث للغتين معًا (hreflang)** — حاليًا اللغة تبديل من جهة العميل (Client-side) بدون روابط منفصلة لكل لغة (`/ar/`, `/en/`)؛ لتحسين ظهور جوجل للنسختين بشكل مستقل، يحتاج الأمر بنية توجيه منفصلة لكل لغة.
3. **الأدوار والصلاحيات (مبيعات مقابل مشرف كامل)** — البنية جاهزة في نموذج البيانات لكن لم تُفعَّل قيود صلاحيات فعلية بعد.
4. **تنبيهات تلقائية** (مثال: عروض أسعار تنتهي صلاحيتها قريبًا) — موجودة كعرض في لوحة المعلومات، لكن بدون إشعار فعلي (بريد/إشعار متصفح).
5. **دعم عملات متعددة بأسعار صرف تلقائية** — حاليًا العملة تُختار يدويًا لكل عرض سعر بدون تحويل تلقائي حي لسعر الصرف.

---

*آخر تحديث لهذا الملف: يعكس حالة الفرع `claude/fetchly-platform-build-h28j9t` بعد إعادة البناء الكاملة من الصفر.*

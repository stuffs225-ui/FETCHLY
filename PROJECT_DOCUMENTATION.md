# توثيق المشروع الكامل — FETCHLY (منصة توريد B2B سعودية)

> ملف مرجعي شامل لمراجعة المشروع بعد إعادة البناء الكاملة (الواجهة + الباك إند الحقيقي). الهدف: تسهيل التقييم وتحديد ما يحتاج تحسين قبل الإطلاق الفعلي. هذا الملف يوثّق **الحالة الفعلية للكود اليوم**، وليس خطة أو نية مستقبلية.

---

## 1. فكرة المشروع (Business Model)

منصة توريد B2B لمنشأة سعودية:

- العميل (شركة أو فرد) يحتاج منتجًا يصعب توفره محليًا.
- يرسل طلبه عبر الموقع **بدون تسجيل أو إنشاء حساب** — صورة، رابط، رقم قطعة (Part Number)، أو وصف مبسّط، بالإضافة إلى بيانات التواصل الأساسية.
- الفريق الداخلي (عبر لوحة التحكم) يراجع الطلب، يبحث عن مصدر مناسب، ويجهز عرض سعر رسمي (PDF).
- عرض السعر يصل للعميل عبر بريد إلكتروني حقيقي، وأي متابعة تجارية تكمل مباشرة.

**ما هو ليس كذلك عمدًا:** ليس متجرًا إلكترونيًا، لا كتالوج منتجات عام، لا حساب عميل، لا سلة شراء، لا دفع إلكتروني عبر الموقع. القسم الوحيد المحمي هو لوحة التحكم الداخلية (`/admin`)، وهو الآن محمي بمصادقة خادم حقيقية وليس كلمة مرور من جهة العميل.

---

## 2. اللغة والتصميم

- **العربية هي الافتراضية** — RTL من الأساس، مع مبدّل لغة كامل (عربي/EN) في الهيدر العام يبدّل `dir`, الخط, ومحاذاة كل عنصر عبر خصائص CSS منطقية (`ps-`, `pe-`, `text-start`...).
- **لوحة التحكم عربية فقط RTL** بدون مبدّل لغة (قرار مقصود — للفريق الداخلي فقط).
- **نظام الألوان (`src/index.css`, Tailwind v4 `@theme`)**: تصميم فاتح بالكامل تقريبًا — خلفيات بيضاء/بيج فاتح (`--color-surface: #f6f4ef`)، نص كحلي غامق (`--color-text: #1e2430`)، لون تمييز واحد مقيّد (كحلي أزرق `--color-primary: #0e4a78`)، والأسود الغامق (`--color-navy: #101826`) محجوز فقط للفوتر / الشريط الجانبي في الإدارة / بطاقة الكرة الأرضية في الهيرو. لا يوجد أي إشارة "ذهبي/فخامة" أو تصميم "AI startup" — التصميم يستهدف الشعور بشركة سعودية حقيقية وعملية.
- المكوّن ثلاثي الأبعاد (الكرة الأرضية، `src/components/three/Globe.tsx`) عنصر ثانوي فقط داخل بطاقة كحلية صغيرة بجانب الهيرو، وليس محور الصفحة.
- خط عربي: **Cairo** (مضمّن أيضًا كخط ثابت في ملفات PDF من جهة الخادم — انظر قسم 7).

---

## 3. البنية التقنية (Architecture)

المشروع الآن تطبيق كامل بواجهة وخادم منفصلين داخل نفس المستودع:

```
FETCHLY/
├── src/                     # الواجهة (React SPA)
│   ├── components/          # ui/, layout/, admin/, three/
│   ├── i18n/                # ar.ts, en.ts, useI18n hook
│   ├── lib/                 # api.ts, apiResource.ts, repo.ts, types.ts, utils...
│   └── pages/                public/, admin/, legal/
├── server/                  # الباك إند (Express + SQLite)
│   ├── index.js             # نقطة الدخول، تركيب كل الراوترات
│   ├── db.js                 # تعريف الجداول + seed أولي
│   ├── lib/                  # auth, email, pdf, pdfTemplate, upload, sqliteSessionStore, crudRouter
│   ├── routes/                auth, requests, quotations, settings, public, users, logs, cms, contact
│   ├── assets/fonts/          خطوط Cairo ثابتة (static instances) مضمّنة في الـ PDF
│   ├── data/                  fetchly.db (SQLite) — لا يُرفع لـ git
│   └── uploads/                ملفات مرفقات الطلبات — لا تُرفع لـ git
├── vite.config.ts           # يوجّه /api إلى localhost:4000 أثناء التطوير
├── .env.example
└── package.json
```

**الواجهة**: React 19 + TypeScript + Vite + React Router + Tailwind CSS v4.
**الخادم**: Node.js (ESM) + Express 5 + better-sqlite3 (قاعدة بيانات ملفية حقيقية، ليست localStorage) + bcryptjs + express-session + nodemailer + multer + Playwright (لتوليد PDF).

### كيف تعمل الواجهة والخادم معًا
- في التطوير: `npm run dev` (Vite على 5173) + `npm run server:dev` (Express على 4000). Vite يوجّه كل طلب يبدأ بـ `/api` إلى الخادم (`vite.config.ts`).
- في الإنتاج: `npm run start` يبني الواجهة (`vite build`) ثم يشغّل خادم واحد يخدم الملفات الثابتة (`dist/`) **و** الـ API معًا على نفس المنفذ.

---

## 4. طبقة البيانات على الواجهة (`src/lib`)

تمت إزالة كل تخزين محلي (localStorage / IndexedDB) نهائيًا. الملفات القديمة (`storage.ts`, `attachments.ts`, `emailService.ts`, `useCollection.ts`, `pdf.ts` الخاص بـ html2canvas) **محذوفة بالكامل**.

- **`src/lib/api.ts`** — عميل fetch بسيط (`api.get/post/put/patch/delete/postForm`)، يرسل `credentials: 'include'` دائمًا (لإرسال كوكي الجلسة)، ويرمي `ApiError` عند فشل الطلب.
- **`src/lib/apiResource.ts`** — مصنع عام (`createResource<T>(basePath)`) يعطي لكل مورد REST: `list/get/create/update/patch/remove` + هوكات React جاهزة `useList()` / `useItem(id)` مع إعادة جلب تلقائية عند أي تعديل على نفس المورد من أي مكوّن آخر (نظام pub/sub داخلي بديل عن أحداث localStorage القديمة).
- **`src/lib/repo.ts`** — يجمّع كل موارد التطبيق (`requestsRepo`, `quotationsRepo`, `casesRepo`, `faqsRepo`, `credentialsRepo`, `savedProductsRepo`, `savedTermsRepo`, `adminUsersRepo`) + دوال مباشرة لعمليات خاصة (`submitSourcingRequest`, `sendQuotation`, `reviseQuotation`, `getPublicCompany`, `getCompanySettings`, `getEmailSettings`, `getAuditLog`, `getEmailLog`...).
- **`src/lib/useAsync.ts`** — هوك عام (`useAsyncData`) لأي قراءة عامة لا تحتاج إعادة جلب تلقائي (بيانات الشركة العامة، بيانات الاعتماد، الأسئلة الشائعة...).
- **`src/lib/adminAuth.ts`** — يغلّف `/api/auth/*` (`login`, `logout`, `fetchCurrentUser`).

**تنبيه للمراجعة:** بما أن أغلب المكوّنات صارت تعتمد على `fetch` غير متزامن، أي مكوّن يعرض بيانات قبل وصول الاستجابة يجب أن يتعامل مع حالة "لا توجد بيانات بعد" بلطف (أغلبها يُرجع `null` ولا يعرض شيئًا، بدل عرض بيانات وهمية — هذا مقصود ومتوافق مع سياسة "لا بيانات مزيّفة أبدًا").

---

## 5. قاعدة البيانات (SQLite — `server/db.js`)

ملف واحد `server/data/fetchly.db` (WAL mode)، يُنشأ تلقائيًا عند أول تشغيل. الجداول:

| الجدول | الغرض |
|---|---|
| `sessions` | جلسات تسجيل دخول المشرفين (بديل express-session المخصص، بدل MemoryStore) |
| `adminUsers` | حسابات لوحة التحكم (bcrypt hash، أدوار `admin`/`sales`، قفل تلقائي بعد محاولات فاشلة) |
| `counters` | عدّادات تسلسلية لأرقام الطلبات وعروض الأسعار |
| `requests` | طلبات التوريد المُرسلة من الموقع العام |
| `attachments` | ملفات مرفقة بكل طلب (تُخزَّن على القرص في `server/uploads/<requestId>/`، المسار فقط في القاعدة) |
| `quotations` | عروض الأسعار (البنود مخزّنة كـ JSON في عمود `itemsJson`) |
| `savedProducts` / `savedTerms` | إعادة استخدام أسعار/شروط سابقة عند بناء عرض جديد |
| `cases` / `faqs` / `credentials` | محتوى CMS (دراسات حالة، أسئلة شائعة، شهادات اعتماد) |
| `companySettings` / `emailSettings` / `contentOverrides` | إعدادات عامة (سطر واحد لكل جدول، `id=1`) |
| `emailLog` | سجل كل محاولة إرسال بريد فعلية — الحالة `sent` أو `failed` فقط (لا يوجد `simulated`)، مع `errorMessage` الحقيقي عند الفشل |
| `auditLog` | سجل نشاط (من أنشأ/عدّل/أرسل ماذا ومتى) |

**قرار تصميمي مهم:** لا يوجد أي حقل نصي بقيمة افتراضية توحي ببيانات حقيقية (مثل `[CR_NUMBER]` أو "قيد الإعداد") — كل شيء يبدأ فارغًا (`''`)، والواجهة العامة تُخفي القسم كاملًا إذا كان فارغًا بدل عرض نص placeholder.

---

## 6. الـ API (كل المسارات تبدأ بـ `/api`)

### مصادقة (`/auth`)
- `POST /auth/login` — `{email, password}`، محدود بمعدّل طلبات (rate limit)، يقفل الحساب بعد محاولات فاشلة متكررة.
- `POST /auth/logout`
- `GET /auth/me` — يعيد المستخدم الحالي أو 401.

### الطلبات (`/requests`)
- `POST /requests` — **عام (بدون مصادقة)**، `multipart/form-data` مع حتى 5 ملفات، يتحقق من الحقول، ثم: يحفظ الطلب، يولّد رقم طلب تسلسلي، يرسل بريد تأكيد حقيقي للعميل + إشعار داخلي (إن كان مُفعّلاً)، ويسجّل كل ذلك في `auditLog`/`emailLog`.
- `GET /requests`, `GET /requests/:id`, `PATCH /requests/:id` — للمشرف فقط.
- `GET /requests/:id/attachments/:attachmentId` — يبث الملف مباشرة (محمي، للمشرف فقط).

### عروض الأسعار (`/quotations`) — كلها تتطلب مصادقة مشرف
- CRUD كامل + `POST /:id/send` (يولّد PDF فعليًا عبر Playwright، يرسله بالبريد، يحدّث الحالة والطلب المرتبط) + `POST /:id/revise` (نسخة معدّلة R1, R2...) + `GET /:id/pdf` (معاينة/تحميل) + `GET /:id/email-preview` (نص البريد قبل الإرسال، قابل للتعديل من الواجهة).

### الإعدادات (`/settings`, مشرف فقط) و(`/public`, بلا مصادقة)
- `/settings/company`, `/settings/email`, `/settings/content` (قراءة/تعديل، مشرف).
- `/public/company`, `/public/content`, `/public/cases`, `/public/faqs`, `/public/credentials` (قراءة فقط، تُصفّي تلقائيًا العناصر غير المنشورة/غير المرئية أو الفارغة).

### إدارة أخرى
- `/users` — إدارة حسابات المشرفين (فقط دور `admin` يمكنه الإضافة).
- `/logs/email`, `/logs/audit` — سجلات للقراءة فقط.
- `/cms/cases`, `/cms/faqs`, `/cms/credentials`, `/cms/saved-products`, `/cms/saved-terms` — CRUD عام موحّد عبر `server/lib/crudRouter.js`.
- `/contact` — نموذج "تواصل معنا" العام، يرسل بريدًا حقيقيًا لبريد الشركة.

---

## 7. المصادقة والأمان

- **bcryptjs** لتجزئة كلمات المرور (لا كلمة مرور تصل للعميل إطلاقًا).
- **جلسات على الخادم** عبر `express-session` مع مخزن SQLite مخصص (`server/lib/sqliteSessionStore.js`) بدل الذاكرة، فتبقى الجلسات صالحة بعد إعادة تشغيل الخادم. كوكي الجلسة `httpOnly`, `sameSite: lax`, و`secure` في الإنتاج.
- **حماية من القوة الغاشمة**: `express-rate-limit` على تسجيل الدخول (20 محاولة/15 دقيقة) + قفل حساب تلقائي بعد 5 محاولات فاشلة متتالية لمدة 15 دقيقة.
- **صلاحيات**: دور `admin` مقابل `sales` (البنية جاهزة، والتحقق مفعّل فعليًا في مسار إدارة المستخدمين — التمييز في باقي الشاشات لم يُبنَ بعد، انظر قسم القيود).
- **حماية عامة**: `express-rate-limit` أيضًا على نموذج الطلب العام ونموذج التواصل لمنع إساءة الاستخدام.
- **الجلسة الأولى**: `ADMIN_EMAIL` و`ADMIN_PASSWORD` في `.env` يُستخدمان مرة واحدة فقط عند تشغيل الخادم بقاعدة بيانات فارغة لإنشاء أول حساب مشرف؛ لا قيمة افتراضية مدمجة في الكود.

---

## 8. البريد الإلكتروني (لا محاكاة إطلاقًا)

- **nodemailer** عبر إعدادات SMTP حقيقية في متغيرات البيئة (`SMTP_HOST`, `SMTP_PORT`, `SMTP_USER`, `SMTP_PASS`, `SMTP_FROM`).
- إن لم تُضبط هذه المتغيرات، كل محاولة إرسال تُسجَّل بصدق في `emailLog` بحالة `failed` ورسالة الخطأ الحقيقية ("SMTP غير مُهيّأ") — **لا يوجد مطلقًا حالة "sent" وهمية**.
- ثلاث حالات إرسال فعلية: تأكيد استلام الطلب، إشعار داخلي عند وصول طلب جديد، وإرسال عرض السعر (مع PDF مرفق فعليًا).
- تم التحقق يدويًا عبر خادم SMTP اختباري محلي أن البريد يصل فعليًا بالمرفق الصحيح (راجع قسم "ما تم اختباره" أدناه).

---

## 9. توليد PDF (من جهة الخادم بالكامل)

- **Playwright + Chromium headless** (`server/lib/pdf.js`) يُحوّل قالب HTML مُولَّد ديناميكيًا (`server/lib/pdfTemplate.js`) إلى PDF عبر `page.pdf()`.
- **الخطوط**: تم تحويل خط Cairo المتغيّر (variable font) من Google Fonts إلى نسخ ثابتة (static instances، وزن 400 و700) باستخدام `fonttools`، لأن Chromium يحوّل الخطوط المتغيّرة تلقائيًا إلى Type3 (خط شبه-صورة غير قابل للتحديد) عند التصدير كـ PDF. النسخ الثابتة مضمّنة كـ base64 مباشرة في HTML — لا اعتماد على إنترنت وقت التوليد.
- **دعم الاتجاه المزدوج (bidi)**: أرقام الجوال/البريد/الموقع مُغلّفة بـ `dir="ltr"` صريح داخل القالب لتفادي مشكلة انعكاس الأرقام (مثل ظهور `+966...` معكوسة) داخل فقرة عربية RTL — تم اكتشاف هذا الخلل وإصلاحه أثناء البناء.
- النتيجة: نص عربي مُشكَّل بشكل صحيح، قابل للتحديد والنسخ، وحجم ملف أصغر بكثير من الطريقة القديمة (كان يعتمد على `html2canvas` + `jsPDF`، أي تصوير الشاشة كصورة).

---

## 10. الصفحات العامة (Public)

الرئيسية (7 أقسام فقط: Hero، مؤشرات ثقة حقيقية، "أرسلها بأي طريقة"، ماذا نوفر، كيف تعمل الخدمة (3 خطوات)، لماذا نحن، دراسات حالة (تظهر فقط إن وُجدت بيانات حقيقية منشورة)، ثم نموذج طلب عرض سعر)، من نحن، ماذا نوفر (صفحة كاملة، 6 فئات + "أخرى"), كيف تعمل الخدمة, الأسئلة الشائعة, الموثوقية والامتثال (`/trust`), تواصل معنا, صفحات قانونية (`/legal/:slug`).

## لوحة التحكم (Admin, كلها خلف `/admin`)

الرئيسية (KPIs + رسم دائري + آخر الطلبات), الطلبات (قائمة + فلاتر + درج تفاصيل), عروض الأسعار (قائمة + محرر كامل), المنتجات المحفوظة, حالات التوريد (CMS), الأسئلة الشائعة (CMS), الشهادات والتوثيق (CMS + رفع مستند كـ base64), محتوى الموقع (تخصيص عنوان الهيرو), إعدادات الشركة, إعدادات البريد (+ سجل البريد), المستخدمون (إضافة مشرف/مبيعات جديد، تفعيل/تعطيل).

---

## 11. متغيرات البيئة (`.env`, راجع `.env.example`)

| المتغير | الغرض | إلزامي؟ |
|---|---|---|
| `PORT` | منفذ الخادم (افتراضي 4000) | لا |
| `NODE_ENV` | `development` / `production` | لا |
| `FRONTEND_URL` | أصل الواجهة (CORS + الكوكي) | نعم |
| `SESSION_SECRET` | مفتاح تشفير الجلسة | **نعم في الإنتاج** (الخادم يرفض العمل بدونه) |
| `ADMIN_EMAIL` / `ADMIN_PASSWORD` | إنشاء أول حساب مشرف مرة واحدة فقط | نعم (أول تشغيل) |
| `SMTP_HOST/PORT/USER/PASS/FROM` | إرسال بريد حقيقي | نعم لتفعيل البريد |

## طريقة التشغيل محليًا
```bash
npm install
cp .env.example .env   # ثم املأ القيم
npm run server:dev     # الخادم على 4000
npm run dev             # الواجهة على 5173 (منفصل)
```
للإنتاج: `npm run start` (يبني الواجهة ويشغّل خادمًا واحدًا يخدم كل شيء).

---

## 12. ما تم اختباره فعليًا (End-to-End)

تم التحقق **داخل متصفح حقيقي (Playwright)**، وليس فقط عبر curl:
1. إرسال طلب عام مع مرفق ملف ← وصل فعليًا لقاعدة البيانات + بريد تأكيد حقيقي (نجح/فشل بصدق حسب إعداد SMTP).
2. تسجيل دخول مشرف حقيقي (جلسة خادم، لا كلمة مرور من جهة العميل).
3. عرض الطلب في لوحة التحكم، إنشاء عرض سعر منه، تسعير البند، حساب VAT صحيح.
4. معاينة PDF (فتح في تبويب جديد) وتحميله (ملف فعلي ~40KB، نص عربي صحيح ومُشكَّل).
5. إرسال العرض عبر خادم SMTP اختباري محلي: **وصل البريد فعليًا** بالموضوع الصحيح والمرفق الصحيح (تم فحصه ببرنامج تحليل بريد فعلي)، وتحديث حالة العرض والطلب تلقائيًا.
6. التحقق من أن محاولة الإرسال بدون SMTP مضبوط تفشل بصدق (رسالة خطأ حقيقية، الحالة تبقى "مسودة"، لا "نجاح" وهمي).

`npm run build` و`npx tsc -b` ينجحان بدون أي أخطاء بعد كل هذا التغيير.

---

## 13. نقاط تحتاج تقييم/تحسين قبل الإطلاق الفعلي

هذه ملاحظات صادقة لتسهيل المراجعة، وليست مشاكل حرجة بالضرورة:

1. **لا اختبارات آلية (unit/integration tests)** — كل التحقق تم يدويًا/بسكربتات Playwright مؤقتة أثناء البناء. يُنصح بإضافة اختبارات API أساسية (خاصة تسجيل الدخول، إرسال الطلب، حساب VAT) قبل الإنتاج.
2. **رفع الملفات على القرص المحلي فقط** (`server/uploads/`) — يعمل تمامًا لخادم واحد، لكن لا يصلح لبيئة متعددة الخوادم (multi-instance) أو حاويات بدون تخزين دائم؛ يحتاج نقلًا لتخزين خارجي (S3 أو ما شابه) قبل التوسّع الأفقي.
3. **قاعدة بيانات SQLite ملفية** — ممتازة لحجم عمل شركة B2B واحدة (مئات/آلاف الطلبات)، لكنها ليست الخيار الأمثل إن احتاج المشروع مستقبلاً نسخًا احتياطية تلقائية سحابية أو قراءة/كتابة من أكثر من خادم في نفس الوقت (PostgreSQL أنسب حينها).
4. **صلاحيات دور "مبيعات" غير مُفعّلة في كل الشاشات** — البنية موجودة (`role: 'sales'`)، لكن حاليًا كل مسارات لوحة التحكم تتطلب فقط "مسجّل دخول" بدون تمييز دقيق للصلاحيات حسب الدور (باستثناء إدارة المستخدمين نفسها).
5. **لا حد أقصى معلن لحجم قاعدة البيانات/الرفعات** — يُنصح بمراقبة حجم `server/data/` و`server/uploads/` وجدولة نسخ احتياطي دوري في الإنتاج (لا يوجد نظام نسخ احتياطي تلقائي حاليًا).
6. **الشعار وصور المستندات مُخزَّنة كـ base64 داخل قاعدة البيانات** (شعار الشركة، مستندات الشهادات) — عملي وبسيط الآن، لكنه يُكبّر حجم قاعدة البيانات مع الوقت إذا كانت الملفات كبيرة؛ يمكن تحويلها لاحقًا لنفس آلية تخزين مرفقات الطلبات (ملفات على القرص + مسار في القاعدة).
7. **لا مراقبة/تنبيهات تشغيلية (monitoring/alerting)** — لا يوجد حاليًا ربط مع أي أداة لمراقبة الأخطاء أو الجلسات الفاشلة في الإنتاج (مثل Sentry)، يُنصح بإضافتها قبل الإطلاق الفعلي.
8. **لم يُختبر تحت حمل حقيقي (load testing)** — كل الاختبار كان بسيناريو مستخدم واحد.
9. **البريد الداخلي/بريد الأعمال ونطاق الموقع لم تُملأ ببيانات حقيقية بعد** — كل حقول الشركة (رقم السجل التجاري، الرقم الضريبي، الجوال، البريد...) فارغة افتراضيًا وتحتاج تعبئة فعلية من `إعدادات الشركة` في لوحة التحكم قبل ظهور أي منها في الموقع العام (وهذا سلوك مقصود، وليس نقصًا في التنفيذ).
10. **حِزم الواجهة الأمامية**: أكبر حزمتين (`index` و`Globe`) تتجاوزان 500KB بعد الضغط بسبب `three.js`/`@react-three/fiber` للكرة الأرضية — تعمل بكفاءة عبر lazy loading لكنها تستحق مراجعة لاحقًا (مثل تحميل الكرة فقط عند التمرير إليها بدل تحميلها ضمن الحزمة الرئيسية إن لم يكن ذلك مطبّقًا بالفعل).

---

## 14. تاريخ التطوير (ملخص المراحل)

1. بناء أولي لمنصة توريد عالمي (تصميم أول، إنجليزي/أمريكي).
2. إعادة بناء كاملة كمنصة سعودية عربية أولاً (RTL، لوحة تحكم، تخزين محلي مؤقت).
3. مراجعة شاملة (20 نقطة من المستخدم) وتنفيذها: تبسيط بصري لتصميم واقعي "شركة سعودية حقيقية" بدل "عرض تصميم AI"، تبسيط الصفحة الرئيسية والنموذج، عرض شرطي صارم لأي بيانات ثقة (لا بيانات وهمية أبدًا).
4. **الأهم**: استبدال كامل الطبقة الخلفية الوهمية (localStorage + IndexedDB + بريد محاكى + مصادقة من جهة العميل + PDF بالتصوير) ببنية إنتاجية حقيقية: خادم Express + قاعدة بيانات SQLite حقيقية + مصادقة جلسات على الخادم + بريد SMTP حقيقي + توليد PDF من جهة الخادم عبر متصفح حقيقي — تم التحقق من كل ذلك عمليًا من البداية للنهاية.
5. **جولة تحسين بصري (UI/UX فقط، بدون أي تغيير في الباك إند أو منطق العمل)** — تمت مراجعة الموقع الفعلي المُصيَّر (وليس الكود فقط) صفحة بصفحة على أحجام شاشة متعددة (1440 / 390) بما في ذلك لوحة التحكم، ونُفِّذت تحسينات مباشرة:
   - تخفيف الكرة الأرضية ثلاثية الأبعاد (والنسخة البديلة SVG على الجوال) — إزالة حركة "النبض" المتنقلة على الأقواس وتخفيف الإضاءة/اللون الذهبي اللامع الذي كان يوحي بمخطط شبكة/بلوكتشين بدل كرة أرضية داعمة هادئة.
   - إعادة تصميم قسم "ما تحتاج تعرف كل تفاصيل المنتج" من 4 بطاقات متطابقة كبيرة إلى صف مسطح بفواصل بسيطة.
   - تقليص قسم "لماذا نحن" من 7 عناصر (بشبكة غير متوازنة) إلى 4 أسباب عملية واضحة بدون بطاقات.
   - إعادة تصميم صفحة "كيف تعمل" المستقلة من جدول زمني عمودي بـ8 دوائر متصلة إلى قائمة تحريرية بعمودين (كل المحتوى محفوظ، فقط العرض تغيّر).
   - إصلاح محاذاة قسم "نماذج من طلبات قمنا بتوريدها" عند وجود حالة واحدة أو حالتين فقط.
   - تقصير تسمية "الموثوقية والامتثال" في شريط التنقل إلى "الموثوقية" لتخفيف الازدحام.
   - **إصلاح خلل بصري حقيقي**: حقل "الكمية" في محرر عروض الأسعار كان يظهر فارغًا بصريًا رغم أن القيمة صحيحة في البيانات (عمود ضيق جدًا لحقل رقمي به أسهم تحكم أصلية) — تم توسيع العمود.
   - **إصلاح خلل بصري حقيقي على الجوال**: قائمة لوحة التحكم الجانبية على الجوال كانت تستخدم تحويل CSS خاطئ لا يلائم اتجاه RTL، مما يترك حوالي ثلث العرض الجانبي ظاهرًا كلوحة فارغة فوق كل صفحات لوحة التحكم على شاشات الجوال — تم إصلاحه.

---

*آخر تحديث لهذا الملف: بعد إتمام إعادة بناء الباك إند وربط الواجهة الأمامية به بالكامل، وبعد جولة التحسين البصري اللاحقة (لا يوجد أي مسار وهمي/محاكى متبقٍ في التطبيق).*
